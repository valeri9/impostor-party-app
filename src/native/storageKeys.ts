/** AsyncStorage keys shared between app code and tests, kept in one place so
 *  the string is never retyped (and never drifts) in more than one spot. */
export const HOWTO_SEEN_KEY = '@impostor_party/seenHowToPlay';
